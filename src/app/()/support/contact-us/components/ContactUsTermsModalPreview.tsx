"use client";

import ContactUsTermsModal from "./ContactUsTermsModal";

export default function ContactUsTermsModalPreview() {
  return <ContactUsTermsModal embedded open onClose={() => undefined} />;
}
