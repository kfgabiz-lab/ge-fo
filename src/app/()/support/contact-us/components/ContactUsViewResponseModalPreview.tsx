"use client";

import ContactUsViewResponseModal from "./ContactUsViewResponseModal";

export default function ContactUsViewResponseModalPreview() {
  return <ContactUsViewResponseModal embedded open onClose={() => undefined} />;
}
